import { Dimensions } from 'react-native'

const winDimensions = Dimensions.get('window')

// Window
export const WINDOW_WIDTH = winDimensions.width
export const WINDOW_HEIGHT = winDimensions.height

// Text
export const TEXT_COLOR_LIGHT = 'white'
